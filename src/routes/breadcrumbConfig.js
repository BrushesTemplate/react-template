import { GOODS_PATH, DEFAULT, ORDERS_PATH } from "./routesPath";

// 面包屑路由配置
const breadcrumbNameMap = {
  [GOODS_PATH]: "商品管理",
  [DEFAULT]: "默认页面",
  [ORDERS_PATH]: "订单管理",
};

export default breadcrumbNameMap;
